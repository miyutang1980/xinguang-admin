# 單車多校接送：部署與操作說明

本版增加「同一台車、同一趟依序接多所小學」，保留既有固定容量、固定班次、假日禁排與完成通知防重複規則。程式已備妥並完成隔離測試，尚待新版 Gateway 部署及配套後台發布；沒有自動合併、搬移或清除正式學生名單。

## 改版後的操作

1. 開啟[正式後台](https://admin.taipingxinguang.org/)，進入「接送排程」。
2. 找到日期及正確路線，例如 B 車下午 15:25；在該列的學校欄按「安排多校」。
3. 加入第一所小學並勾選要接的學生。
4. 按「新增學校」，加入第二所或更多小學，再分別勾選學生。
5. 用「上移／下移」調整停靠順序。未勾選任何學生的學校只是暫存編輯區塊，不會保存為接送點。
6. 核對整車合計及上限，按儲存。主表顯示如「第一國小 → 第二國小」。

例如 B 車第一校 1 人、第二校 2 人，整趟合計 3／4 人，而不是每校各有 4 個名額。各校學生顯示姓名及小學班級，雙語姓名沿用當學期名冊。

附上的隔離操作預覽使用示範帳號 `demo`、密碼 `demo`，登入後進「接送排程」並選「所有週次」，可找到 2026/10/02 B 車的兩校範例。預覽資料是合成的，儲存只在預覽記憶體內生效，重新載入即還原，不連正式系統。

## 車次與資料保護

- **整車容量**：A 車 6、B 車 4、C 車 4、小巴 8、半巴 4；走路無上限。固定規則自 2026/09/29 起適用，沒有因新增學校而增加容量。
- **固定時間**：週一、四 12:25／15:25；週二僅 15:25；週三僅 12:25；週五六條路線均另有 14:45 獨立趟次，原 12:25／15:25 保留。既有假日禁止排車規則不變。
- **同時段重複**：同日、同一班次且相同出發時間，同一位學生若仍在另一車次，儲存前阻擋。週五 14:45 與 15:25 不會被誤認為同一趟。
- **不自動搬移**：同畫面兩列都顯示 B4，不代表可自動合併。要依路線識別核對原學生歸屬；系統不會因調整學校而替你移走其他車次的學生。
- **完成後保留**：已接回的班次不能再改學生與接送點；中午已完成不會封鎖尚未完成的下午。
- **學校摘要**：由學生明細推導，不再透過單一學校下拉選單獨立改寫。舊資料不會在部署時批次遷移。

## LINE 通知

通知依第 1 站、第 2 站等分組，列出各校要接與取消人數、學生中英文姓名與班級，並保留全車總數。仍以整趟車為單位回報「已接回」，不是每所小學各發一則完成通知；既有通知事件鍵及重試機制不變。

沒有排定學生的空車趟不發接送通知。LINE 內容與首次／重複回報已用 mock LINE 測試，尚未向正式群組實發驗證。

## 部署順序

### 先更新既有 Gateway

1. 先備份目前 Apps Script 的「程式碼.gs」。
2. 用本次附件 `Gateway-單車多校接送-20260924.txt` 的全文取代「程式碼.gs」，儲存。不要新增第二份相同主程式，不要覆蓋測試／救援檔。
3. 右上角「部署」→「管理部署」→選目前正式 Web app →鉛筆圖示。
4. 「版本」選「新版本」；說明填：`單車多校接送 v1｜學校排序、整車容量、依校通知、防重複｜2026-09-24`。
5. 按「部署」，保留既有部署網址，不另建新的部署網址。
6. 回覆「多校 Gateway 已部署」，再核對能力回應及發布配套後台。

版本核對沿用既有 `routes_policy_capabilities`，應新增 `multiStopVersion: "multi-stop-v1"`；原 `policy.version: "fixed-pickup-v1"` 與 `transport-notice-v1` 保持相容。這次不需要新增或重建觸發器。

### 再發布配套後台

配套後台修改位於 `xinguang-admin` 的 `fix/multistop-pickup-20260924` 分支，本文件交付時尚未推送到正式 main。新版後台會拒絕舊 Gateway，以免學校摘要或容量檢查不一致；因此必須先完成 Gateway。

發布目標是[管理後台](https://admin.taipingxinguang.org/)，不是公開招生網站。正式後台上線後按 Ctrl＋Shift＋R，再檢查「安排多校」按鈕。

## 正式驗收與限制

- 選一筆尚未接回、固定規則無衝突的真實車次，經校方核對後安排兩所小學並重開確認。
- 若原列仍有「固定班表不符」提示，先完成既有固定班表的預覽與套用；部署程式不等於已套用班表。不要刪除或重建現有學生明細來繞過限制。
- 原先保留的特殊加班車仍維持保護，不會自動變成固定範本或套入本次多校編輯。
- 正式 LINE 實發及實際接回回報仍需在真實車次與適當時間由校方確認，不用測試函式對真實群組連發。

## 隔離測試紀錄

後端 53 項檢查涵蓋既有固定班表回歸與多校讀寫、排序、總容量、跨車重複、完成後鎖定及單次完成通知。介面 15 項檢查涵蓋選校、勾學生、排序、儲存重開、取消、超載攔截、版本檢查及桌面／手機尺寸；不代表已登入正式後台完成端到端驗收。
