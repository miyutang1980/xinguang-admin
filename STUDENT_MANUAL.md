# 學生主檔與學期班級指派操作手冊

更新：2026-09-22 · 前端介面 v2.20.0

## 先確認啟用狀態

新版介面不代表 Gateway 或資料移轉已完成。若顯示「資料已暫存 · 尚未啟用」，目前只能核對資料，不能新增或修改。若顯示「無法載入｜已停用寫入」，代表無法取得完整資料，**不是空名冊**。

管理員須先完成 Gateway 部署、資料移轉驗收與新模型啟用，才能正式維護。本手冊不宣稱生產環境已部署或啟用；請依實際驗收結果確認。不要為了繞過錯誤改用舊名冊或 N8N 寫入。

## 一、資料分工

| 介面 | 內容 | 原則 |
|---|---|---|
| 學生管理 · 主檔 | 綠色學生及家長基本資料 | 跨學期沿用，每位學生只建一次 |
| 學期班級指派 | 黄色班務及當期就學資料 | 同一學生每學期一筆 |
| 115-1 | 目前營運的新學期 | 啟用後可新增、編輯及整班調整 |
| 114-2 與其他歷史學期 | 當期歷史快照 | 僅查閱，不新增、不編輯、不刪除 |
| 舊「在校學生」 | 既有請假、接送等流程的相容名冊 | 由 Gateway 維護，新介面不直接寫入 |

主檔修改與學期變動不同。更換家長電話改主檔；轉班、老師、課程、就讀學校、接送或停學改目前學期指派。新增主檔不代表已加入 115-1。

## 二、學生主檔

1. 開啟「學生管理 · 主檔」，先以姓名、學生編號或帳號搜尋，避免重複建檔。
2. 新生按「新增學生主檔」；既有學生按「查看／編輯」。
3. 填入或修正資料，姓名必填，電話與識別碼以文字保存，保留前導零。
4. 按「儲存」。系統先重新核對目前資料，再儲存並重新讀取確認；離開欄位不會自動儲存。
5. 新生需要參加課程時，另到「學期班級指派」建立 115-1 指派。

### 全部欄位

原 24 欄完整呈現，另新增「弋果分校」，共 25 欄：

- 學生編號、學生帳號、學生姓名、英文名、性別、生日、身分證字號、Email。
- 住家地址、備註/飲食禁忌。
- 媽媽姓名、媽媽手機、媽媽公司電話、媽媽工作單位、媽媽Email。
- 爸爸姓名、爸爸手機、爸爸公司電話、爸爸工作單位、爸爸Email。
- 媽媽LINE userId、爸爸LINE userId。
- 建立時間、更新時間。
- 弋果分校。

**學生編號、建立時間及更新時間為系統唯讀欄位**；其餘 22 欄可於模型啟用後編輯。學生編號為永久關聯鍵，不能用姓名或工作表列位置取代。學生帳號須符合 Gateway 規則且不可重複。歷史指派的姓名、帳號保留當期快照。

## 三、新增或修改 115-1 指派

1. 開啟「學期班級指派」，確認學期為「115-1 · 新學期（可編輯）」。
2. 使用學期、學期狀態、班代號及關鍵字查詢。
3. 新增時按「新增學期指派」，可先輸入姓名／學號篩選，再從已建檔主檔選擇學生。
4. 已有 115-1 指派的學生不會再列入新增選項；要修改時請編輯原紀錄。
5. 核對黃色欄位後儲存。學生編號、帳號及姓名由主檔帶入，指派編號及更新時間由系統管理。
6. 暫時不就讀時，依實際狀況將「學期狀態」改為停學、休學、離校或其他適用值，不刪除主檔或指派。

### 17 個黃色欄位

1. 學期（本版固定 115-1）
2. 弋果班級（穩定班代號）
3. 弋果課程
4. 學生類別
5. 學校
6. 年級（就讀學校年級）
7. 小學班級
8. 外籍教師
9. 中籍教師
10. TXClass（課程級數／班級顯示名稱）
11. 學年度起始日
12. 課堂時間
13. 課程類別
14. 教室
15. 學期狀態
16. 課後輔導
17. 交通車

其餘 5 欄為指派編號、學生編號、學生帳號、學生姓名及更新時間。每筆指派的唯一鍵為「學期｜學生編號」。

學期狀態建議值：在學、在讀、停學、休學、退學、離校、畢業、結業、未入學，最終由 Gateway 驗證。

課後輔導原值為 `NO`、`課輔`、`托育`；交通車原值包含 `家長接送`、`Walk 走路`、`交通車`。保留已存在的其他受支援值，不會因打開表單就強制替換。請勿把托育視為「無課輔」而覆寫。

### 個別轉班

修改該生的「弋果班級」班代號。介面會帶入目標班既有的共用資料；新班沒有其他學生時會清除舊班的共用欄位，請重新核對。TXClass 在同一學期同一班代號應一致；若後端提示衝突，請先用整班調整修復，不要逐人強制改出不同級數。

## 四、查看 114-2 歷史

1. 學期切換為「114-2 · 歷史（唯讀）」。
2. 可使用狀態、班代號及搜尋篩選，再按「查看歷史」。
3. 歷史視窗全部欄位唯讀，沒有儲存按鈕，也沒有新增或整班升級入口。
4. 114-2 不會自動搬到 115-1；切回 115-1 才能處理新學期。

歷史校正需另行由管理員審核及保留紀錄，本版不提供歷史修改功能。

## 五、整班升級／調整 SOP

本功能以 **115-1 ＋ 弋果班級班代號** 決定範圍，不以學生姓名、畫面搜尋結果或 TXClass 舊顯示名稱作關聯。

1. 在 115-1 按「整班升級／調整」，選擇穩定班代號。
2. 勾選需要更新的欄位。課程升級通常勾選 TXClass 並輸入目標級數；本版不自動推算升級。
3. 可一起修改弋果課程、課程類別、中籍教師、外籍教師、學年度起始日、課堂時間、教室。
4. 未勾選欄位保留原值；**勾選但留空會清空該欄**。
5. 按「預覽全班變更」，逐一核對班代號、全班筆數、學生名單及每欄前後差異。
6. 列表的搜尋／狀態篩選不縮小整班範圍，停學、休學、離校等紀錄也在該班範圍內。
7. 勾選確認聲明，按「確認更新全班」，再確認系統對話框。
8. 系統重新核對預覽後是否有人修改資料，再送出一次 class-wide API；成功後重新讀取確認，不逐人循環寫入。
9. 若預覽過期，重新預覽；若儲存結果不明，先關閉視窗、重新整理核對，勿直接重送。

不修改學生主檔、不更改班代號、不調整小學年級、不變動 114-2 或其他班。**這不是跨學期複製或自動建立下一學期**。115-2 等未來學期的匯入、啟用與開學作業需另行設計及管理員處理。

## 六、錯誤與例外

| 畫面／情況 | 處理方式 |
|---|---|
| 資料已暫存 · 尚未啟用 | 僅核對；由管理員完成 Gateway 部署、移轉驗收及啟用 |
| 無法載入／後端尚未升級／欄位不完整 | 不是零筆成功；停止操作，請管理員核對版本與表頭 |
| 已成功讀取但顯示 0 筆 | 僅代表所選清單或篩選沒有紀錄；與預期不符先查移轉，不要大量重建 |
| 登入憑證失效 | 登出後重新登入；不要共用或把密碼放進網址、手冊、程式碼 |
| 其他人已修改資料／列號變動 | 關閉視窗，重新整理後再編輯 |
| 重複指派／重複帳號 | 找到既有資料再修改；同名學生以學生編號區分 |
| 同班 TXClass 衝突 | 用整班調整修復一致性 |
| 逾時、儲存結果尚未確認 | 不宣稱成功；重新整理核對，避免重複新增 |

新介面沒有刪除學生、刪除指派或姓名配對批次覆寫功能。

## 七、管理員部署與驗收清單

以下為待執行驗收，不是已完成生產部署的證明。

1. 確認 Gateway 已支援所有新 action，工作表表頭及文字格式一致。
2. 驗收主檔去重、25 欄完整性、115-1 與 114-2 指派對應及歷史快照。
3. 確認新模型尚未啟用時，清單可核對但所有寫入被前後端共同阻擋。
4. 新模型正式啟用後，確認主檔、新增／編輯指派、整班調整及舊流程相容名冊同步。
5. 前端需一起部署 `index.html`、`student-semesters.js`、`student-semesters.css`、本手冊；不得只更新 HTML。
6. 測試缺少 action、回傳 HTML、缺少 list、錯誤學期、重複識別碼、失效登入、網路失敗。不得假裝成零筆或寫入成功。
7. 測試桌面與手機、歷史唯讀、取消整班預覽、取消最終確認、預覽後他人修改，以及同一按鈕連點。
8. 驗收使用隔離測試資料，不把真實学生／家長資料或密碼存入 repository、截圖測試樣本或提交紀錄。

### API 對接

通用驗證參數：`adminUser`、`adminPass`，沿用既有 Gateway transport；`fields` 使用工作表中文標題，update 使用 `row_index`。

| Action | 用途／額外參數 |
|---|---|
| `student_master_list` | 主檔清單 |
| `student_master_create` | `fields`；回傳 `studentNo` |
| `student_master_update` | `row_index`, `fields` |
| `semester_assignments_list` | `semester`；回傳 `semester`, `semesters`, `list` |
| `semester_assignment_create` | `fields`；回傳 `assignmentNo` |
| `semester_assignment_update` | `row_index`, `fields` |
| `semester_class_update` | `semester`, `classCode`（弋果班級）, `fields`；回傳整數 `updated` |

清單須有 `success: true` 及完整陣列，每列包含 `_row` 與所有表頭。`active: false` 表示模型尚未啟用；舊相容回應省略 `active` 時，仍需通过其餘完整性檢查，寫入權限最終由後端把關。前端不以 `list || []` 將失敗偽裝成空名冊。
