# 接送速度修正：版本化明細與單次儲存

本次不是再延長等待時間，而是減少反覆查詢與多餘寫入。先前只把讀取上限拉長，不能當成速度改善；正式帳號的首次載入時間仍需部署後實測。

## 本次改動

- **版本化明細**：Gateway 對每一趟的排程與學生明細產生版本指紋。後台取得完整資料後可在同次登入共用五分鐘，不因 30 秒過期又查一次。
- **衝突保護**：儲存帶回原始版本，Gateway 在鎖定期間、寫入前比對。其他人已修改同趟資料時，拒絕覆蓋並要求重新讀取。
- **一次儲存**：Gateway 原本已寫入總人數，後台不再發第二次人數回寫。儲存回應直接附上已核對的最新明細與版本，不再立即重查。
- **成段寫入**：連續的學生列以一段寫入，間隔中其他車次的列不動。保留整車容量、固定班次、假日禁排及完成通知防重複機制。
- **相容模式**：舊 Gateway 沒有資料版本時仍使用原 30 秒共用規則，不假裝新機制已生效。頁面主版號為 `20260924.4`，需搭配下列 Gateway 版本才啟用五分鐘共用。

## 這次只需更新既有 Gateway

1. 開啟[Gateway Apps Script 專案](https://script.google.com/home/projects/1a58uIi0Zbtxr6esICbVuE8CM9i7gtdozANhnu1SLUfhTrwNHAtqB_5lN/edit)。
2. 備份「程式碼.gs」，用附件 `Gateway-接送明細版本快取與批次儲存-20260924.txt` 全文取代並儲存。不要附加在舊程式後面，也不要改測試／救援檔。
3. 「部署」→「管理部署」→既有正式 Web app →鉛筆→「新版本」。
4. 說明填：`接送效能 v1｜版本快取、衝突保護、單次儲存｜2026-09-24`。
5. 按部署，保留原網址，不新增觸發器。
6. 回覆「速度版 Gateway 已部署」，再核對正式能力回應及同一筆車次的讀取。成功後重新整理後台一次即可。

能力回應應同時有 `multiStopVersion: "multi-stop-v1"`、`detailsReadVersion: "pickup-revision-v1"`；後台會自動使用新版資料指紋。[正式能力檢查入口](https://script.google.com/macros/s/AKfycbw-7_a_OfUVlgegcLxkux_9dr9UlYSVKhi3uQjV-0sr2X2TpRRmCXtSM7jbIqMHK4hNww/exec?_action=routes_policy_capabilities)

## 已完成的隔離驗證

後端共 60 項檢查通過，前端共 58 項檢查通過，含既有接送規則回歸及新版本機制。新增的版本化操作案例結果：一次批次讀取後隔 90 秒開明細，新增單筆明細查詢 0 次；成功儲存請求 1 次，額外人數寫入 0 次；他人改動版本後，拒絕舊資料覆蓋。

以上是合成資料測試，不是正式登入帳號的網路計時。冷啟動、第一次讀取、五分鐘過期或手動重新整理仍需 Gateway 查詢；這版主要消除工作中反覆開啟同趟造成的重讀，不能承諾首次載入必定幾秒。

## 資料安全與現況

部署本身不搬移學生、不新增班表、不改觸發器，也不發送 LINE 測試通知。若儲存發生版本衝突，先重新取得資料核對，不要重複送出舊內容。
