# 後台效能檢查與 3 秒改善方案

檢查日期：2026-09-23。目標網站：[太平新光後台](https://admin.taipingxinguang.org/)。本輪處理共用前端瓶頸，保留原有正式資料與權限驗證；不宣稱全系統第一次查詢及正式儲存已達 3 秒。

## 結論

等待約 30 秒有一個明確的共用原因：舊 Gateway 包裝在等待 15 秒後，會再自動嘗試一次。另有搜尋時重讀雲端、重複請求、頁面切換重拉學生清單，以及登入後自動健檢等額外工作。

本次已修正上述共用問題。但移除重試只能避免重複等待，不能把後端執行時間直接降到 3 秒。正式站雲端瀏覽器單次唯讀探測：開班資料 HTTP 200 約 10.106 秒；行事曆設定等待 15.001 秒後取消。這是單次、未登入的外部測試，不是管理員實際網路、冷啟動原因分析或正式效能分位數。

## 本次已實作

- **Gateway 共用請求**：移除自動重試；只有明確列入白名單的讀取可使用 60 秒記憶體快取、共用相同進行中請求。HTTP 錯誤與明確業務失敗不快取。
- **寫入安全**：即使用 GET 傳送的修改，也不快取、不合併、不自動重送。修改前後使共用快取失效；逾時不可視為成功。
- **帳號隔離**：共用快取依登入狀態隔離，登出立即清除；學生檢視快取也在登出清除。未新增學生資料的持久化儲存。
- **六類搜尋**：問課、預約、體驗預約、夏令營、分級檢測、正式報名，改為篩選已載入的表格。保留輸入焦點與條件，不逐字呼叫後端。
- **學生主檔與學期指派**：同一登入的檢視資料可共用 5 分鐘，畫面顯示資料取得時間；手動重新整理讀最新資料。儲存前核對、一次寫入、儲存後讀回仍保留。
- **重複同步**：同一登入中同步 Google Sheets 的重複操作共用一次請求組合；各讀取有 15 秒期限，舊登入回應不更新目前資料。
- **系統健檢**：不再登入前後自動發動整組健檢。手動健檢的 3 項服務與 5 個表頭查詢並行，重複開啟共用一次工作；離開頁面後不覆蓋別頁。
- **頁面切換**：已載入模組直接顯示；預約及體驗預約加入過期畫面回應保護。
- **診斷紀錄**：共用 Gateway 記憶體中最多保留 100 筆 action、耗時與結果類別，不記錄網址、密碼、學生欄位或請求內容。這不是所有 API 的完整監控。

## 全後台範圍盤點

以下為程式與共用資料路徑檢查，不等於每個正式 CRUD 功能均已使用真實帳號逐項實測。導覽權限清單有 24 項，另檢視程式中的公告模組。

| 功能群組 | 本輪處理或現況 | 尚需後端驗證 |
|---|---|---|
| 儀表板、LINE 來源分析 | 檢查既有本機/同步資料路徑，同步去重 | 首次同步與 LINE 資料來源 |
| 問課、預約、夏令營、分級檢測、正式報名、體驗預約 | 六類已載入表格改本機搜尋；Gateway 讀取套用共用改善 | 首次讀取及各類正式新增、修改、通知 |
| 學生管理、班級指派 | 5 分鐘檢視快取；儲存仍新鮮核對及讀回 | 改為後端單次條件式更新，減少三次往返 |
| 接送排程 | 沿用先前當學期與短期名冊共用；跨模組異動失效測試通過 | 正式 Gateway 的當學期範圍讀取版本需部署/核對 |
| 今日接送、強制轉交 | 已檢查，仍含額外轉交資料來源；今日接送有串行等待 | 主畫面與次要資料拆開；未知轉交狀態不可顯示成零件 |
| 老師月報 | 目前為說明/資料表入口，不是即時統計 API | 若需即時月報，另建索引/彙總端點 |
| 體驗課排班、開班資訊、參訪/檢測日曆 | 納入共用 Gateway 讀取改善；保留既有排班快取機制 | 首次讀取、儲存鎖定、版本/失效策略 |
| 請假、潛客、公告 | 明確讀取動作納入共用層；寫入不重送 | 查詢範圍、分頁、通知耗時及模組個別錯誤呈現 |
| 說明會簡報、文件中心、連結中心 | 檢查頁面路由；不是本輪 30 秒 Gateway 重試的主要修正點 | 外部文件載入不計作已驗證 3 秒 |
| 帳號管理 | 帳號清單不使用新增的共用快取；保留驗證 | 正式權限變更、帳號資料與登入端點效能 |
| 系統健檢 | 改按需、併行、去重與過期回應保護 | 外部服務探測本身可能超過 3 秒 |

## 測試結果與限制

離線 Playwright 模擬 API 測試中，已載入主檔切回約 0.132 秒、重訪約 0.068 秒、編輯視窗約 0.049 秒；合成快速後端下的預約與體驗預約約 0.077、0.082 秒。這些數字只驗證前端路徑，不代表正式 Google Apps Script 或 Sheets 的速度。

8 組瀏覽器回歸測試涵蓋登入、接送排程、115-1 恢復、接送明細、資料連動、讀取延遲、名冊效率、全系統共用效能。兩組 Gateway 離線測試涵蓋當學期範圍、原始列號、權限、缺少設定與資料不一致時停止操作。測試未寫入正式資料。

新增測試確認六類搜尋無額外資料請求、重複讀取合併、錯誤不快取、登入隔離、修改不自動重送、過期讀取失效、讀取只等一次 15 秒、健檢八項同時啟動且不覆蓋其他頁，以及同步共用一次工作。桌面及手機畫面檢查保留原有可橫向捲動表格。

尚未取得正式 Apps Script 的完整目前程式、執行記錄與可部署授權，因此沒有以舊版本覆蓋正式後端，也沒有操作真實學生/帳號資料來測儲存。先前準備的分學期 Gateway 更新不能視為已正式上線。

## 達成「真正 3 秒」的下一階段

### 後端先做的工作

- **量測每段時間**：每次請求給 requestId，量測驗證、鎖等待、資料讀取、寫入、通知與總耗時。不要只量畫面出現或 HTTP 200。
- **只讀所需範圍**：115-1 日常操作只查該學期；歷史保留後台。名冊、接送依學期/日期/班級限定範圍，不掃全歷史再由前端過濾。
- **批次讀取與索引**：同一次工作批次取得需要的欄位，避免逐列或逐學生呼叫 Sheets。清單增加分頁/必要欄位投影。
- **一次完成安全儲存**：前端提交記錄 ID、預期版本、變更欄位與冪等鍵；後端在鎖/交易內檢查版本、更新、確認持久化，回傳新的記錄與版本。版本衝突需明確拒絕，不能移除核對來換速度。
- **拆開次要工作**：必要資料成功保存後即可回報「已儲存」；LINE、PDF、通知等後續工作另行處理並顯示狀態。只有入列尚未落庫時不得宣稱已儲存。
- **縮短競爭**：驗證與資料範圍讀取不要無差別重做；鎖只保護必要修改區段。不得用跨帳號共享權限資料換取速度。

若完成上述改造後，Apps Script 路徑的正式 P95 仍超過 3 秒，再評估以具索引的資料庫/API 作為交易主資料來源，Sheets 改作報表鏡像。這屬於架構與資料移轉，需另行備份、核對差異、設定切換與回復方案，不在本輪偷偷搬移。

### 驗收定義

| 動作 | 目標 | 正確的完成定義 |
|---|---|---|
| 已載入列表切換、搜尋、開啟編輯 | 3 秒內 | 可操作的正確資料，不是轉圈或空殼 |
| 第一次登入、第一次查詢 | P95 ≤ 3 秒 | 真正通過授權且取得所需最新資料 |
| 修改、更新 | P95 ≤ 3 秒 | 後端確認保存並回傳版本；不能先顯示假成功 |
| 慢速或失敗 | 立即顯示進度/可恢復錯誤 | 逾時不等於失敗未寫入，不自動重送修改 |

每個主要路徑至少收集 30 次真實使用樣本，區分首次/重訪、尖峰/離峰與測試裝置；記錄失敗與超時，不能從統計中排除。先累積基準值再比較改善，3 秒是驗收目標而非這次已完成的保證。

## 發布與回復

本輪修改以前端 Git 版本發布；重新整理後應取得 `student-semesters.js?v=2.20.3`。若需回復，將本輪修正 revert 後重新部署，回到前一版 `f696bec`，不清除正式試算表、不刪歷史學期，也不在不確定儲存結果時重送操作。
