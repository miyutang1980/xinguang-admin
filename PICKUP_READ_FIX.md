# 多校接送明細讀取逾時修正

2026/09/24 使用者截圖顯示多校視窗已開啟，但 `routes_get_details` 於 20 秒被中止，造成無法選校及儲存。本次只修改後台讀取流程，不修改 Gateway、不搬移學生、不發送 LINE 通知。

## 修正範圍

- 完整的批次明細保留 30 秒，須含可核對的學生編號、姓名、學校及相符人數，不能以畫面上的姓名摘要代替。
- 快取限定本次登入、學期、名冊版本、讀取代次、日期、路線、列號及班次，不存入瀏覽器持久儲存。
- 正在取得的批次結果與單筆請求可共用，避免重複查詢。失敗不展開逐列補查，也不轉成空名單。
- 接送排程、人員、單筆／批次明細，以及本流程的名冊／姓名唯讀查詢統一使用 60 秒上限；寫入期限及不自動重送規則不變。這是容許較慢回應，不是承諾讀取速度。
- 「重新取得資料」繞過已完成快取。重新載入、寫入與名冊更新會使相關快取失效；舊請求不得回填新版本。
- 儲存仍走原有 Gateway 驗證，不增加自動重送，不放寬整車容量或已接回保護。

## 驗證

沿用 15 項多校介面測試，另外完成 17 項慢讀取與快取保護測試，包括模擬 25 秒回應成功、超過 60 秒明確報錯、跨帳號隔離、資料缺漏／人數不符不可共用、重複請求合併與失敗不重送。測試使用合成名冊與隔離 API，不是正式帳號的延遲測量。

## 分段載入修正 20260924.3

後續截圖顯示 `routes_get_staff` 超過原本 20 秒，舊的 Promise.all 讓員工清單失敗連帶取代整張已取得的班表。本版把人員查詢與班表的必要讀取分開，不再讓可獨立處理的人員下拉清單阻斷學生安排。

- 人員等待／失敗時，保留已取得的班表、既有司機姓名及學生明細；停用司機／隨車人員下拉欄位。
- 名冊與班表都通過原有驗證後，學生明細及多校安排才可使用，不因人員查詢降級而繞過名冊驗證。
- 提供「重試人員名單」，只重試該查詢，不重讀班表或學生名冊，不寫入任何正式資料。
- 人員恢復時僅更新選單，不重建整張表，不干擾已開啟的多校視窗。
- 重新整理時立即鎖定舊畫面控制，等新班表核對完成再啟用，過期讀取不得回填。
- 頁首顯示「介面版本：接送分段載入 20260924.3」，便於核對是否載入新版。

追加 16 項分段載入檢查，合計 48 項前端回歸通過。涵蓋人員慢回應、503 錯誤、60 秒逾時、單獨重試、既有司機保留、名冊失敗仍鎖定學生編輯、班表／權限失敗不顯示可編輯資料，以及桌面和手機畫面檢查。

## 使用者操作

正式後台更新後，關閉舊的多校視窗，按 Ctrl＋Shift＋R，再開啟「接送排程」與「安排多校」。若仍超過 60 秒，保留錯誤畫面與日期／路線／班次，繼續調查 Gateway 讀取效能；不可將錯誤名單視為零人後儲存。
